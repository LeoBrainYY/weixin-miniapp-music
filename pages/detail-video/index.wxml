<!--pages/detail-video/index.wxml-->
<video  class="video" 
        src="{{mvURLInfo.url}}"
        danmu-list="{{danmuList}}"
        autoplay>
</video>

<block wx:for="{{100}}" wx:key="*this">
  <view>列表数据{{item}}</view>
</block>
